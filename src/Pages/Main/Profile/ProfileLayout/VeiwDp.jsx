import React from "react";

const VeiwDp = ({curr}) => {
   console.log(curr);
   return (
      <div>
         {/* <img src={curr} alt="" /> */}
         {curr}
      </div>
   );
};

export default VeiwDp;
